# BASE_2주차_과제 2
## Seq2Seq
### 1. LSTM 기반 Seq2Seq 모델에서 디코딩할 때 사용하는 Beam Search 동작 방식에 대해서 설명.

>Beam Search는 디코딩 시 탐색 공간을 줄이면서도 더 좋은 결과를 얻기 위한 **휴리스틱 기반 탐색 알고리즘**이다. 기본적인 Greedy Search(가장 확률 높은 단어 하나만 선택)보다 더 나은 결과를 제공한다.

1. 디코더가 시작 토큰 `<sos>`을 받아 첫 번째 step에서 각 단어의 확률을 출력함.
2. 확률이 높은 상위 k개의 토큰(Beam width)을 선택하여 후보 시퀀스를 유지함.
3. 각 후보 시퀀스마다 다음 step에서 가능한 단어 확률을 계산하고, 누적 로그 확률을 기준으로 다시 상위 k개 후보만 유지함.
4. `<eos>` 토큰이 등장하거나 최대 길이에 도달할 때까지 반복함.
5. 최종적으로 누적 확률이 가장 높은 시퀀스를 선택함.

```
Seq2Seq 모델은 번역이나 요약 같은 걸 할 때, 문장을 한 단어씩 만들어 감. 매 순간 가장 확률이 높은 단어 하나만 고르는 Greedy Search가 가장 쉬움. 그 순간엔 좋아 보여도 나중에는 문장이 이상해질 수 있음. 그래서 등장한게 Beam Search.
```

```
Beam size가 3이라면, 매 시점마다 확률이 높은 상위 3개 문장 후보를 유지하면서 계속 단어를 추가해 감. 모델이 예측한 단어들 중 가장 확률이 높은 3개를 고르고, 또 각 후보에 대해 또 다음 단어를 예측함. 그 후 모든 후보들 중에서 누적 확률이 높은 세 개만 남김. 이렇게 문장이 끝날 때 까지 반복하는 것.
```


### 2. Seq2Seq with LSTM 모델은 Attention이 없던 시절 제안된 구조임. 기본 Seq2Seq 모델의 한계와 이후 Attention 메커니즘이 이 한계를 어떻게 보완했는지 설명.

- 기본 Seq2Seq 모델의 구조\
인코더가 입력 시퀀스를 모두 보고 고정된 컨텍스트 벡터 하나로 요약. 디코더는 이 벡터 하나만 참고하여 전체 출력을 생성.

- 기본 Seq2Seq 모델의 한계.
1. 정보 압축 손실\
고정된 벡터 하나로 긴 문장의 의미를 모두 담는 데 어려움이 있었음. -> 장문일수록 성능이 저하되는 문제 발생.

2. 기억력 부족\
입력 문장의 특정 부분을 참조할 수 없고, 전체 요약만 가지고 디코딩하는 문제 발생.

- Attention 메커니즘의 보완
1. 디코딩 시 입력 시퀀스의 각 위치마다 **어디를 집중할지** 학습함. 즉, 디코더는 각 스텝마다 인코더의 모든 hidden state를 활용해 **가중 평균**을 계산함.
2. 결과적으로, 디코더가 동적으로 중요한 부분에 초점을 맞출 수 있게 됨. 긴 문장에 대한 처리 능력 향상. 성능에 있어 크게 개선됨.
